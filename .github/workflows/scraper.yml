name: Daily Keyword Scraper

on:
  schedule:
    # 매일 UTC 23:00 (한국 08:00), UTC 09:00 (한국 18:00)
    - cron: '0 23,9 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 저장소 체크아웃
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: 파이썬 설정
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: 라이브러리 설치
      run: |
        pip install -r requirements.txt

    - name: 크롤러 실행
      env:
        NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
        NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
        COUPANG_ACCESS_KEY: ${{ secrets.COUPANG_ACCESS_KEY }}
        COUPANG_SECRET_KEY: ${{ secrets.COUPANG_SECRET_KEY }}
        NAVER_AD_CUSTOMER_ID: ${{ secrets.NAVER_AD_CUSTOMER_ID }}
        NAVER_AD_ACCESS_KEY: ${{ secrets.NAVER_AD_ACCESS_KEY }}
        NAVER_AD_SECRET_KEY: ${{ secrets.NAVER_AD_SECRET_KEY }}
      run: |
        python main.py

    - name: 결과물 강제 푸시
      run: |
        git config --global user.name 'GitHub Action Bot'
        git config --global user.email 'action@github.com'
        
        # 1. 봇이 만든 파일 스테이징 및 커밋
        git add output/data.json
        git commit -m "🤖 봇: 데이터 자동 업데이트" || exit 0
        
        # 2. [핵심] 다른 거 묻지도 따지지도 않고 강제로 덮어씌우기
        # pull, rebase 이런 거 안 합니다. 무조건 밀어버립니다.
        git push -f origin main
