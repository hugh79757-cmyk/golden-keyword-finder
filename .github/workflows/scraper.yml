name: Daily Keyword Scraper

on:
  schedule:
    - cron: '0 21 * * *' # 매일 한국시간 오전 6시
  workflow_dispatch: # 수동 실행 버튼

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 저장소 체크아웃 (전체 기록 가져오기)
      uses: actions/checkout@v3
      with:
        fetch-depth: 0 # <--- [핵심] 봇에게 "과거 기록까지 다 가져와"라고 시키는 옵션

    - name: 파이썬 설정
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: 라이브러리 설치
      run: |
        pip install -r requirements.txt

        - name: 크롤러 실행
      env:
        NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
        NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
        # 아래 두 줄 필수 추가!
        COUPANG_ACCESS_KEY: ${{ secrets.COUPANG_ACCESS_KEY }}
        COUPANG_SECRET_KEY: ${{ secrets.COUPANG_SECRET_KEY }}
      run: |
        python main.py


        - name: 결과물 커밋 및 푸시
      run: |
        git config --global user.name 'GitHub Action Bot'
        git config --global user.email 'action@github.com'
        
        # [핵심] 현재 위치를 main 브랜치로 명확하게 고정합니다.
        git checkout main
        
        # [핵심] 최신 변경사항을 먼저 당겨옵니다.
        git pull origin main
        
        # 데이터 파일 추가 및 커밋
        git add output/data.json
        git commit -m "🤖 봇: 데이터 자동 업데이트" || exit 0
        
        # 최종 푸시
        git push origin main

